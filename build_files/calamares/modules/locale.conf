---
# Default locale and timezone pre-selected in the UI.
# The locale module handles language, timezone, and (optionally) keyboard.
region:   America
zone:     New_York

# Write locale settings to the installed system.
localeConfig: /etc/locale.conf

# Don't manage keyboard — let the user configure it in KDE settings.
configureKeyboard: false
