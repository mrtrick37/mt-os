name: Build Live ISO

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-live-iso:
    runs-on: ubuntu-latest
    container:
      image: fedora:39
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install dependencies
        run: |
          dnf install -y docker just xorriso squashfs-tools mtools dosfstools git
          systemctl start docker || true

      - name: Ensure permissions and directories
        run: |
          mkdir -p output/live-iso
          chmod -R 777 output
          chmod -R 777 build_files
          chmod +x build_files/build-live-iso.sh
          chmod +x build_files/build.sh

      - name: Build Live ISO
        run: |
          just build-live-iso

      - name: Upload ISO artifact
        uses: actions/upload-artifact@v4
        with:
          name: kyth-live-iso
          path: output/live-iso/kyth-live.iso
          if-no-files-found: error
